#ifndef CONFIG_H
#define CONFIG_H

// WiFi Configuration
const char* WIFI_SSID = "YourNetwork";
const char* WIFI_PASSWORD = "YourPassword";

// Sonos Speaker Configuration
const char* SONOS_IP = "192.168.x.x";     // Your Sonos speaker IP
constexpr int SONOS_PORT = 1400;
constexpr int VOLUME_STEP = 2;

// Hardware Configuration (TTGO T-Display)
constexpr int BUTTON_A_PIN = 35;
constexpr int BUTTON_B_PIN = 0;

// Display Configuration
constexpr uint8_t DISPLAY_ROTATION = 1;       // Landscape: 240x135, USB on left
constexpr bool DISPLAY_INVERT = true;         // Set false if colors invert

// Timing Configuration
constexpr uint32_t DEBOUNCE_MS = 35;
constexpr uint32_t LONG_PRESS_MS = 700;
constexpr uint32_t REPEAT_INTERVAL_MS = 300;  // Auto-repeat while held

#endif // CONFIG_H
