#ifndef CONFIG_H
#define CONFIG_H

// WiFi Configuration
static const char* WIFI_SSID = "YourNetwork";
static const char* WIFI_PASSWORD = "YourPassword";

// Sonos Speaker Configuration
static const char* SONOS_IP = "192.168.x.x";  // Your Sonos speaker IP
static const int SONOS_PORT = 1400;
static const int VOLUME_STEP = 1;

// Hardware Configuration (TTGO T-Display)
static const int BUTTON_A_PIN = 35;
static const int BUTTON_B_PIN = 0;

// Rotary Encoder
static const int ENCODER_CLK_PIN = 25;  // A
static const int ENCODER_DT_PIN = 26;   // B
static const int ENCODER_SW_PIN = 27;   // Button

// Display Configuration
static const uint8_t DISPLAY_ROTATION = 1;       // Landscape: 240x135, USB on left
static const bool DISPLAY_INVERT = true;         // Set false if colors invert

// Timing Configuration
static const uint32_t DEBOUNCE_MS = 35;
static const uint32_t LONG_PRESS_MS = 700;
static const uint32_t REPEAT_INTERVAL_MS = 300;  // Auto-repeat while held

#endif // CONFIG_H
